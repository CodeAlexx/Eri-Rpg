---
name: coder:switch-project
description: Switch to another project mid-session
argument-hint: "<project>"
---

<action>
Run this command to switch projects:

```bash
python3 -m erirpg.cli switch $ARGUMENTS
```

This saves the current session and switches to the target project.
The statusline will update to show the new active project.
</action>
