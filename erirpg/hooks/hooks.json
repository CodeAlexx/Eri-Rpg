{
  "description": "EriRPG enforcement hooks - blocks Edit/Write without active preflight",
  "install": "Copy to ~/.claude/settings.json or merge with existing hooks config",
  "note": "Set ERIRPG_ROOT env var or use full path to pretooluse.py",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${ERIRPG_ROOT:-/home/alex/eri-rpg}/erirpg/hooks/pretooluse.py",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
