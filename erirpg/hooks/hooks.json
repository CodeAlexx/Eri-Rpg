{
  "description": "EriRPG enforcement hooks - blocks Edit/Write without active preflight",
  "install": "Copy to ~/.claude/settings.json or merge with existing hooks config",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -m erirpg.hooks.pretooluse",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
