---
name: eri:status
description: Show current run status and provide resume command
argument-hint: "<project>"
---

<action>
Run this command to check status:

```bash
python3 -m erirpg.cli status $ARGUMENTS
```

This shows active runs, current phase, and what to do next.
</action>
