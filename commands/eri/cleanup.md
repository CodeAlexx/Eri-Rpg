---
name: eri:cleanup
description: Clean up stale runs and state
argument-hint: "<project> [--prune]"
---

<action>
Run this command to cleanup:

```bash
python3 -m erirpg.cli cleanup $ARGUMENTS
```

Add `--prune` to actually delete stale runs.
</action>
