---
name: eri:index
description: Index/reindex project codebase
argument-hint: "<project> [--force]"
---

<action>
Run this command to index:

```bash
python3 -m erirpg.cli index $ARGUMENTS
```

This builds the dependency graph needed for standard/full tier features.
</action>
