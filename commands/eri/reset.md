---
name: eri:reset
description: Reset EriRPG state to idle
argument-hint: "<project>"
---

<action>
Run this command to reset state:

```bash
python3 -m erirpg.cli reset $ARGUMENTS
```

This clears all run state, preflight state, and quick fix state.
</action>
