---
name: eri:defer
description: Capture an idea for later implementation
argument-hint: "<project> <idea> [--tags v2,perf]"
---

<action>
Run this command to defer an idea:

```bash
python3 -m erirpg.cli defer $ARGUMENTS
```

To review deferred ideas:
```bash
python3 -m erirpg.cli deferred <project>
```
</action>
